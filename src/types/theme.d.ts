export type ComponentVariants = "primary" | "secondary" | "base";

export type ComponentVariantProps = {
    bg: string;
    color: string;
    hoverBg: string;
    hoverColor: string;
    activeBg: string;
    activeColor: string;
    shadow: string;
};